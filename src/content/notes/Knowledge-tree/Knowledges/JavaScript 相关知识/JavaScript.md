# 基础知识
## 数据类型
### 1.原始类型
```js
number：数字，例如 42、3.14。
string：字符串，例如 "Hello"。
boolean：布尔值，例如 true、false。
null：空值，表示没有对象值。
undefined：未定义，表示变量已声明但未赋值。
symbol（ES6 新增）：唯一且不可变的数据类型，主要用于对象属性的键。
```
### 2.引用类型：
```js
object：对象，包含多个属性和方法。
array：数组，有序集合。
function：函数，可执行的代码块。
```