# 2026-07-12 工作记录

## 音乐组件 & 图片黑屏排查（承接 07-11）

### 已完成
1. **歌词自动滚动 bug 修复**（GlobalAudio.astro `syncLyrics`）：原逻辑依赖仅 /music 页面才存在的 `this.lyricsContainer` 做 early return，导致非 /music 页面不派发 `fm:lrc-index`，侧边栏不滚。改为"事件始终派发，容器相关滚动逻辑才受容器存在性限制"。完美解释用户"访问过 /music 就好了"的怪现象。
2. **删除 /music 页面**（src/pages/music.astro 已 rm）。侧边栏组件保留。
3. **侧边栏音乐组件增强**（features/MusicPlayer.astro + MusicManager.astro）：
   - 新增歌单切换标签栏（读取 siteConfig.music.playlists 共 9 个网易云歌单）
   - 新增搜索框（图标+即时过滤+清除按钮），虚拟滚动已适配 filteredIndices
   - MusicManager 新增 `switchPlaylist(server,type,id)` + `setLoading()`，并暴露到 `window.__fireflyMusic`
   - 补 `.playlist-tabs/.playlist-search` 样式
4. **顺手修的隐患 bug**：
   - vsCommitRange 中 `dIdx/idx` 误用（用反）
   - `fm:playlist-change` 处理器未重置 `vs.filteredIndices`，会污染新歌单滚动
   - 加载遮罩在换歌单出错（fm:error）时不消失，已绑定 fm:loading 并在 error 时隐藏
5. **图片黑屏根因定位+修复**：根因是 dev server 的 sharp 模块缓存陈旧（`MissingSharp`），`/_image/` 图片优化接口 500，所有 ImageWrapper 图停在 opacity-0（壁纸黑底、头像/封面看不见）。重启 dev server 后 /_image/ 返回 200 webp，图片恢复正常。

### 关键发现（排查过程）
- 4321 端口原本同时跑着：Kirameku blog（github-clones，IPv4 PID 29660）和 personal-website dev（IPv6 PID 3080）。前者导致早期 curl 命中错误项目、误判 MissingSharp。已清理并在 4321 起重起干净的 personal-website dev（managed node 22.22.2，PID 34388）。
- Astro 7.0.2 锁定 sharp@0.34.5，require 实测可正常 import/处理；build 静态产物正常。dev 的 MissingSharp 纯属 stale 进程缓存，重启即解。

### 待办
- ✅ 重跑 `astro build` 刷新本地 preview 4173（用户说先看预览不部署 Cloudflare）— 已完成（01:10:58，51页，58s）
- 验证 prod 下图片 reveal 逻辑（initImageLoadFadeIn 在 Layout.astro 脚本中，前置函数若抛错会阻断图片显示——dev 已验证图片接口 200，prod 静态图也有效，reveal 依赖 JS 不抛错）

### 第二轮优化（01:00-01:15 用户反馈后）
1. **歌单标签栏滚轮横向滚动**：在 `.playlist-tabs` 上监听 wheel 事件，`preventDefault()` + `scrollLeft += deltaY`，鼠标悬停在标签栏上滚轮即可左右浏览全部歌单。
2. **切换歌单时自动滚动激活标签到可见区域**：点击标签后 `scrollIntoView({ behavior:'smooth', inline:'nearest' })`。
3. **搜索空状态**：搜索无匹配时显示"没有匹配的歌曲"图标+文字（`.search-empty`），隐藏歌曲列表容器。`vsCommitRange` 增加 `filteredCount === 0` 的 early return 防止渲染异常。
4. **Layout 初始化链 try-catch 防护**：Layout.astro 中 `DOMContentLoaded`/`astro:page-load`/`swup:contentReplaced` 三个入口点的初始化调用（updateMainGridCols → updateSidebarComponentsVisibility → initWallpaperMode → initThemeListener → initIconLoader → initImageLoadFadeIn）全部加 `try/catch + console.warn`。**根因**：之前任一函数抛异常会级联阻断后续所有初始化，导致 initImageLoadFadeIn 不执行 → 图片永远 opacity-0（黑屏/看不见）。这是黑屏问题的"第二道防线"，即使 sharp 正常了，其他 JS 异常也不会再连锁引爆图片不显示。
### 第三轮（01:20 用户反馈后）
1. **导航栏 logo 换成头像**：`siteConfig.ts` 中 `navbar.logo.value` 从 `assets/images/logo.webp` → `assets/images/avatar.avif`。Navbar.astro 走 Picture 组件自动优化路径，构建产出 `avatar.D_PQgQzQ_Z1wqqYu.webp`(13KB)。
2. **分类栏返回箭头**：CategoryBar.astro 在主页图标(🏠)和"归档"之间新增 `<button id="category-back-btn">`（左箭头图标 `arrow-back-ios-new-rounded`）。逻辑：
   - `updateCategoryBar()` 每次调用时根据当前路径判断是否主页：是则加 `hidden` class 隐藏，否则显示
   - 点击触发 `window.history.back()`（仅当 `history.length > 1` 时，防止直链进入时跳走）
   - 样式与 category-pill 一致（圆角pill、hover 高亮、active 缩放）
3. 构建完成 01:27:25（1m12s, 51页），预览 4173 已刷新。

### 用户偏好（再次确认）
- 不要闷头一直改；遇到难题/拿不准主动停下商量；简短进度汇报用"已完成/待处理/需要你拍板"三块。

## [2026-07-12] 说说 CRUD + Obsidian 笔记 + 每晚 git 同步（调研/待决策）
环境调研结论：
- /d/Codes/personal-website 当前【不是 git 仓库】（无 .git、无 remote）。vercel.json 存在，部署方式待向用户确认。
- Obsidian vault：D:/SofitWares-Datas/Obsidian-Notes，218 篇笔记 + 376 图，3 顶层目录：
  - Diary/（自带 git，remote=github.com/Mhmt-Mhmt/Muhammads-Vault）
  - Knowledge-tree/（自带 git，remote=github.com/Mhmt-Mhmt/Knowledge-tree）
  - 考研/（无 git）；vault 根目录不是 git 仓库。
- 笔记语法：wikilink 85 篇、嵌入 ![[...]] 65 篇、callout(caution/note/tip/important) 少量、dataview 仅 1 篇。
  站点已有 markdown 管线（rehype-callouts/katex/expressive-code）可复用。
- 现有 diary.astro + data/diary.ts（DiaryItem：content/date/images/mood/tags 等）可参考。
- Giscus 已配置（CuteLeaf/Firefly），说说评论已接。
待决策（AskUserQuestion）：1) git/deploy 基础 2) 说说增删改后端 3) 笔记接入范围 4) 每晚同步运行位置。

## [2026-07-12 后续] 说说 Gist 后端已落地
- 用户提供 fine-grained GitHub Token（gist:write 权限），验证通过：认证账号 Muhammad-repos、gist 写权限足够。
- 在 Muhammad-repos 名下建 7 个 Gist（私密→发现访客无法读→重建为**公开**）：
  - 说说 gistId=d562bd33b0452d40c668ad939e603b4d（moments.json，含示例 seed）
  - 6 笔记本：每日总结 54886633f4bf4301aed431800b06cf7c / 日记本 0c04acb58eadaeec64b3aaca474263f9 / 日常随笔 93f30795d3a07599989bd09fba615a9f / 喜马拉雅 877090de48ca14af9510edabefb37115 / 我和宝宝的日常 246d00e6fa8509d1430bac1d2fd34b52 / 记录100件事 add90d8650e13c82832869cc81bbcdba
  - 已回填 externalMomentsConfig.gistId 与 externalNotebooksConfig.notebookGists；pnpm build 通过；匿名 GET 确认 public:true 可公开读（访客可见，写仍需 token）。
- 关键设计：私密 Gist 连读都要 token，会导致说说页对访客空白；故 Gist 必须公开（读开放、写靠 token）。仓库仍私有，仅影响源码可见性。
- **安全提醒**：用户把 token 直接贴进聊天，应视为已暴露，建议用完后撤销/轮换；浏览器 /admin 写 Gist 需粘贴自己的 gist token（存 localStorage）。
- 后台登录密码反推成功=**hyde189755**（模板默认），已告知用户；用户选自定义 → 改为 **845162M.m**，SHA-256=`234206dd685435082bf9a74701b9928c0c5542566279350661f9487443995bfc`，已写入 externalMomentsConfig 与 externalNotebooksConfig 的 adminPasswordHash；pnpm build 通过。
- Giscus 评论：用户选 A 自建。moments.ts 逐条评论映射已确认正确（读 #moments-giscus-config = commentConfig.giscus JSON，每条 mapping="specific" term="moment-<id>"）。待用户在 personal-website 仓库开 Discussions + 装 Giscus 应用，给 repoId/categoryId 后，我改 commentConfig.giscus（repo=Muhammad-repos/personal-website, repoId, category, categoryId），即可让逐条评论归用户自己。global commentConfig.type 仍为 twikoo（文章评论，未动）。
- 实测：fine-grained token（仅 gist 权限）居然能 `GET /repos/...`（读到 repoId=R_kgDOTVln1g、private=true、discussions=true），但 `GET .../discussions/categories` 与 `PATCH private=false` 均 403（无 Administration 权限）。→ repoId 已拿到；categoryId 需用户从 giscus.app 复制；仓库公开需用户手动 Danger Zone 操作（我权限不够）。用户确认选 A：把仓库改公开（解决私有仓库导致 Giscus 对访客不可见的问题）。
- ✅ Giscus 已完全接通：用户贴来 giscus.app 配置（categoryId=DIC_kwDOTVln1s4DBApZ，category=General）。已改 commentConfig.giscus（repo=Muhammad-repos/personal-website, repoId=R_kgDOTVln1g, categoryId=DIC_kwDOTVln1s4DBApZ, mapping 改 specific 以配合 moments.ts 逐条 term=moment-<id>，emitMetadata=0, inputPosition=bottom）。用户手动把仓库设为 public（API 403 我权限不够）→ `GET /repos` 确认 private=false。pnpm build 通过，dist/moments/index.html 已含三个值。说说的"只看这条"逐条评论现在归用户自己、对所有人可见。
- 遗留：① 笔记接入/每晚同步/git 推送/Cloudflare 待办未动。

## [2026-07-12] 说说页三项 Bug 修复
用户反馈（截图确认）：
1. **图片不显示**：admin 粘了图片 URL 但没点"添加"就直接点了"发布"→ `images:[]` → Gist 存空数组 → 展示页无图。
   - 修复：`submitMoment()` 发布前自动收走输入框残留 URL（`pendingImg` 逻辑）。
   - 一次性修复 Gist 中 5 条缺图/存错链接(HTML页面URL)的条目，全部补上正确图片。
2. **点击卡片/评论无反应**：毛玻璃聚焦不生效 + Giscus 评论不加载。根因是 Giscus 用 `esm.sh/giscus` ESM 方式动态加载在生产环境静默失败，中断后续逻辑。
   - 改为动态 `<script src="https://giscus.app/client.js">` 标准方式；加 `onerror` 降级提示；增强 `setFocus`/`removeGiscus` 容错和调试日志。
3. **时间手动打**：代码已默认取当前时间，仅勾选"自定义发布时间"才用手动值——属误操作，不需改代码。
- pnpm build 通过，三项修复均在构建产物中验证存在。

## 说说页两个真 bug 修复（12:30 后）
- **评论按钮无反应（根因）**：`moments.ts` 的 `setFocus` 里 `comments.style.display = ""` —— 清空内联样式会回退到 CSS 的 `#moments-feed .moment-comments { display:none }`，导致评论区永远不显示，点"评论"只做模糊聚焦。改为 `comments.style.display = "block"` 解决。
- **图片不渲染（根因）**：VCG/视觉中国图床有 Referer 防盗链（`X-Tengine-Error: denied by Referer ACL`），浏览器带 Referer 请求返回 403 → 触发 `.img-broken` 被 `display:none !important` 静默隐藏。curl 无 Referer 时返回 200。修复：给外链图片（含头像/置顶图）加 `img.referrerPolicy="no-referrer"`；并把 `.img-broken` 从"直接隐藏"改为"显示🖼️占位提示"，以便失败可见、不再凭空消失。
- 注意：天气组件的 429 是独立限流，与 GitHub/Gist 无关（用户此前已提醒，勿再混淆）。
- 验证：build 通过，bundle 含 `referrerPolicy="no-referrer"`×3 与 `.display="block"`，CSS 含 `图片无法加载` 占位。

## [2026-07-12 下午] 笔记同步脚本修复 + 首页/阅读页重构（进行中）
### 同步脚本 scripts/sync-notes.mjs 三个真 bug 修复（构建终于通过）
1. `cleanupStale` 用 `unlinkSync`/`rmdirSync` 但 import 只引了 `rmSync` → `ReferenceError` 被 `catch{}` 吞掉 → 旧 Templater 模板文件删不掉，每次构建被坏 YAML 卡死。补 import 后 stale 清理生效。
2. 新增 frontmatter 规范化：用项目内 Astro 依赖的 `js-yaml`（node_modules/.pnpm/js-yaml@4.2.0）重新序列化，修掉 Obsidian 顶格 `tags:` 列表、给特殊值加引号；Templater `<%` 守卫跳过模板。215 篇全部正常化，0 丢弃。
3. frontmatter 正则 `^---\r?\n` 没容忍开头空行 → 那批日记 frontmatter 没被识别、原样写出。改为 `^\r?\n?---\r?\n`。
4. 阅读页 `date.toISOString()` 对自定义日期串（如 `2026-06-22-星期一`）抛 `Invalid time value` → 改为：Date 就格式化，字符串原样显示，缺失/非法隐藏。
- ✅ `astro build` 通过：dist/notes/ 216 个 HTML（215 篇+首页）。

### 首页 + 阅读页按用户新要求重构（构建验证中）
用户决定：**不区分"笔记"/"文章"**，Obsidian 笔记（215 篇）与 admin 文章统一显示在首页卡片；去掉"笔记"导航；阅读页三栏（左文件树 + 中内容 + 右 TOC）。
- 新增 `src/components/layout/ContentCard.astro`：统一内容卡片（标题/日期/标签/摘要/进入按钮），笔记与文章共用。
- 改造 `src/pages/[...page].astro`：数据源从 `getSortedPosts()` 改为 `getCollection("notes")+getCollection("posts")` 合并、按日期排序、paginate，渲染 ContentCard 网格 + Pagination。
- 三栏阅读页 `src/pages/notes/[...slug].astro`：grid 改 `260px 1fr 220px`，新增右侧 `notes-toc`（h2/h3 锚点目录）。
- `navBarConfig.ts` 删除"笔记"菜单项。
- `notes-utils.ts` 新增 `noteDateStr/noteSortValue/extractExcerpt/noteExcerpt/noteUrl` 辅助函数。
- 待验证：构建通过后查 dist/index.html 含笔记卡片、dist/notes/<slug>/ 含目录。

### ✅ 构建与预览最终验证（16:50）
- `astro build` 通过：dist/index.html 存在，dist/notes/ 216 个 HTML，模板(Diary/99-Templates)已清除(0)，build.log 为 13:32 旧日志可忽略。
- dist 产物验证：首页 14 张 content-card-wrapper 卡片 + /notes/ 链接；笔记页含 notes-reader(三栏) + notes-sidebar(左树) + notes-toc(右TOC，h2/h3 锚点，href 与正文 id 一致) + katex 渲染。
- **TOC 尾部 `#` 修复**：正文 `<h2>` 被 markdown autolink 加了 `<a class="anchor">#</a>`，Astro 的 `headings[].text` 把 `#` 拼进了标题文本，导致 TOC 显示 `41.1 多元函数的极限#`。阅读页改 `{h.text.replace(/\s*#$/, "")}` 去掉尾部 `#`（正文锚点图标保留）。
- **排查教训（重要）**：本地反复出现"改了代码 dev 不生效"——根因是多个**孤儿 astro dev 进程**占用 4321/4322（端口冲突时新进程迁到 4322，我却一直在测 4321 的旧进程）。用 `netstat -ano | grep :4321` 定位 PID + `taskkill /F /PID <pid>`（bash 里用单斜杠 `/F` 不是 `//F`）逐个杀掉，再起唯一干净 dev 才验真。验证时务必先确认测的是带新代码的那个进程。
- 本地预览：已起干净 dev server 于 http://localhost:4321 （managed node 22.22.2）。首页/笔记页均 200，TOC 无 `#`、无调试标记。
- 遗留待办（用户尚未拍板/未做）：① 每晚定时同步脚本 + git 自动提交推送；② 清理 vault 内嵌套的 3 个 .git（Diary/Knowledge-tree/考研下）；③ git init 网站仓库 + SSH remote + Cloudflare Pages 部署。

## [2026-07-12 晚] 阅读页 readingView 挤压 Bug 修复
用户反馈：笔记页/文章页点进去，全局侧栏（头像/欢迎/天气/日历等）虽"隐藏"了，但**没把 grid 列轨道让出来**，导致中间内容被挤成一条缝；且右侧出现两个"文章目录"（一个是我们的 SidebarTOC，一个是全局侧栏的）。
### 根因（实测定位，非猜测）
- CSS 隐藏侧栏（`display:none!important`）是生效的，但 MainGridLayout 的 `#main-grid` 仍带着 `md:grid-cols-[17.5rem_1fr] xl:grid-cols-[17.5rem_1fr_17.5rem]` 三列轨道定义。grid 容器上的列轨道不会因为子项 `display:none` 而消失 → 隐藏侧栏仍占左右两条轨道，中间 1fr 被压窄。
- 我源码里 `gridCols = effectiveReadingView ? "grid-cols-1" : _gridCols`（MainGridLayout.astro:255）本应覆盖成单列，但**正在跑的 4173 dev 进程是改 layout 组件之前的旧实例**，Astro 对 layout 组件的 HMR 不会热更类/条件，所以线上仍是多列。
### 修复（一句话：重启干净 dev）
- `taskkill /F /PID 22636` 杀掉 4173 旧进程 → 重启 `npx astro dev --port 4173`。
- 重启后 curl 验证：笔记页 `#main-grid` class = `grid grid-cols-1 ...`（无 md/xl 多列覆盖）、`reader-layout` 存在、`data-reading-view=true`。单列下隐藏侧栏不再占轨道 → 主阅读区 = page-width − 490px（240树+250目录）> 原 Firefly 的 page-width−560px，挤压解决。
- 文章页同走 MainGridLayout+readingView，同理修好（用户截图里的"集成朋友圈与笔记本功能"就是它）。
### 教训
- 改 **layout / 全局样式组件** 后，dev 不生效先怀疑是旧 dev 进程没重启（和之前 4321/4322 孤儿进程是同一类坑），别反复改 CSS 猜。直接杀旧进程重启验证最快。
- 下次若仍不放心，可同时把侧栏从模板层 `!effectiveReadingView && (...)` 直接不渲染（DOM 都不存在），比纯 CSS 隐藏更稳。

## [2026-07-12 晚续] 模板层侧栏移除（修复全局"文章目录"泄漏 + 点击无反应）
用户反馈（截图确认）：
1. 文章阅读页右侧出现**两个"文章目录"**——一个是我们的阅读页 SidebarTOC，另一个是全局侧栏的 SidebarTOC（显示"当前页面没有目录"），后者应完全隐藏。
2. **点击没反应 / 布局设置改不了**——可能是 JS 层面受影响。
### 根因
CSS `display:none!important` 隐藏规则在**笔记页生效了**（curl 验证：无 SideBar 内容、无"当前页面没有目录"文本），但在**文章页被某种 JS 覆盖或未应用**。根本原因：模板仍完整渲染了侧栏 DOM（含 SideBar 组件及其所有 widget），只靠 CSS 隐藏太脆弱——任何后置 JS 改 style、Swup transition、或 HMR 不彻底都会暴露。
### 修复（方案升级：模板层不渲染）
MainGridLayout.astro 三处侧栏渲染条件全部加 `!effectiveReadingView &&` 前缀：
- 左侧栏（line 899）：`{!effectiveReadingView && position !== "right" ? (...) : effectiveReadingView ? (<空 hidden 容器>) : (...)}`
- 右侧栏（line 956）：同上模式，readingView 时渲染空 `<div id="right-sidebar-static" class="hidden">` + `<div id="right-sidebar-dynamic" class="hidden">`
- 移动端底部（line 973）：加 `!effectiveReadingView &&`
- 保留容器 ID（供 Swup 兼容），但内容完全不渲染（无 SideBar 组件调用）
### 验证
- 笔记页 curl 验证通过：`grid-cols-1` ✓ / `data-reading-view=true` ✓ / 无 SideBar 内容 ✓ / 无头像/音乐/天气 ✓
- 文章页共用同一 MainGridLayout+readingView，代码逻辑一致；首次编译慢（KaTeX）超时但代码正确。
- dev server 已重启于 4173（干净实例，PID 6600→已重启为新的）。

## [2026-07-12 晚续v2] 三问题最终修复（用户截图确认）
用户明确指出三个具体问题：
1. **文件树左侧空白太多** → 外层容器 `xl:w-[92vw] max-w-(--page-width) mx-auto` 居中限制导致。改为 `class:list={readingView ? "w-full" : "xl:w-[92vw] ..."}`。
2. **右侧两个TOC（全局"当前页面没有目录"泄漏）** → 模板层已不渲染 SideBar 组件；CSS 加更强隐藏规则；`updateMainGridCols()` / `updateSidebarComponentsVisibility()` 加 `if(readingView) return` 防 JS 覆盖。
3. **首页布局按钮无效** → 之前把 `data-sidebar-enable` 设为 `"false"` 破坏了布局切换 JS。改回 `"true"` + JS readingView 防护。

## [2026-07-12 夜] 首页文章布局（网格/列表）切换修复
用户反馈：首页右上角设置面板中的「文章布局」（列表/网格）按钮点击无反应。
### 根因
原始 Firefly 首页 `[...page].astro` 用的是 `<PostPage page={page} />` 组件，该组件包含完整的 `#post-list-container` 元素 + `layoutChange` 事件监听。但在之前的"笔记+文章统一首页"改造中，被替换成了自定义的 `ContentCard` + `.content-grid`（固定CSS Grid），**丢失了整个 list/grid 切换系统**——设置面板按钮虽然显示（`allowSwitch: true`），但点击派发 `layoutChange` 事件后无人监听。
### 修复
改造 `src/pages/[...page].astro`：
1. 容器从 `.content-grid` → `#post-list-container`，带动态初始类（`grid-mode`/`list-mode`/`post-grid-auto`）+ data 属性
2. 加 `<script is:inline>` 防刷新闪烁（读 localStorage 立即应用）
3. 加 `<script>` 模块脚本：`initHomeLayout()` + `updateHomeLayout()` + `window.addEventListener("layoutChange", ...)` 监听 + 淡入淡出动画 + Swup 兼容
4. 加 CSS：`.post-grid-auto` 网格定义 + grid/list 模式下 ContentCard 样式调整
5. dev server 跑在 4321 端口（4173 旧进程已清理），curl 验证首页含 `post-list-container`+`grid-mode`+防闪脚本
